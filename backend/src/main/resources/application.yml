spring:
  application:
    name: HotelM

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB

  datasource:
    url: jdbc:mysql://localhost:3306/HOTELM_DB?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: redis

  mail:
    host: smtp.gmail.com
    port: 587
    username: dinhgiang19092k6@gmail.com
    password: fxfu herh ydbq eiup
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

otp:
  expiry-seconds: 300

server:
  port: 8080

hotel:
  upload-dir: uploads

jwt:
  secret: your-256-bit-secret-key-change-this-in-production-minimum-32-chars
  expiration: 86400000

rate-limit:
  enabled: true
  api:
    capacity: 100
    refill-tokens: 100
    refill-duration-seconds: 60


