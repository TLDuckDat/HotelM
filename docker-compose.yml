version: '3.8'

services:
  mysql:
    image: mysql:oracle
    container_name: mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: HOTELM_DB
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always

volumes:
  mysql_data: